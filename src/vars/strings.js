const errMsg = `[HTTP]: An error has occurred`;
const hitMsg = `[HTTP]: Client hit endpoint: `;
const uploadErrMsg = `${errMsg} while trying to upload this file.`;

module.exports = {
    errMsg, 
    hitMsg,
    uploadErrMsg,
}